/**
 * Represents a user object for display in UI components
 * Used in UserDropdown, DashboardHeader, and other components
 */
export interface CustomUser {
	name: string;
	email: string;
	avatar: string;
}
