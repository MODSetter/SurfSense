{
  "name": "surfsense-local-electron",
  "private": true,
  "version": "0.0.1",
  "description": "Electron shell: spawns the API + worker sidecars, loads the Vite SPA",
  "main": "./out/main/index.js",
  "scripts": {
    "dev": "concurrently -k -n vite,electron -c cyan,magenta \"pnpm --dir ../frontend dev\" \"wait-on tcp:127.0.0.1:5173 && electron-vite dev\"",
    "build": "electron-vite build",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "check:sidecars": "node scripts/check-sidecars.mjs"
  },
  "devDependencies": {
    "@types/node": "^24",
    "concurrently": "^10",
    "electron": "^44",
    "electron-vite": "^5",
    "typescript": "~6",
    "vite": "^7",
    "wait-on": "^9"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["electron", "esbuild"]
  }
}
