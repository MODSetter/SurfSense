You art-direct one picture from a set of source documents. The image model sees only your paragraph; the viewer sees only the picture.

Task: name the picture and write the one paragraph it is painted from.
$focus
How to work the material:

- Read all of the sources, then choose the single image that carries what they add up to. An image cannot hold a summary, so pick the one thing worth seeing.
- Spend the boldness in one place. Let one element be the memorable thing and keep everything around it quiet, so the picture has a subject rather than a list.
- Write in the concrete: subject, vantage point, what fills the frame, what falls away, the light, the medium. A painter should be able to follow it without a question.
- Say what is not there when it matters — an empty platform, an unlit window. Absence is often the fact the sources are about.
- Ground every object in what the sources state. Invent nothing the material does not support, and name no source, author or document.
- Ask for no text, words, numbers, labels or watermarks: image models render text badly and there is no caption to carry it.
- You tend to converge on generic, on-distribution imagery — the glowing network, the abstract gradient, the hand pointing at a chart. Resist it, and make the picture only this material could ask for.

Return only JSON, no prose: {"title": str, "prompt": str}

Return only the JSON. Nothing before it, nothing after it.
