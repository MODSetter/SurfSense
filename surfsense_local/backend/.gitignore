.venv
__pycache__/
.pytest_cache/
.ruff_cache/
*.egg-info/

# PyInstaller onedir output (bundling/) — rebuilt per OS, never committed
/dist/
/build/

# Embedding, voice, and parser packs staged for packaging (scripts/fetch_*.py models)
/models/

# Datasets the retrieval eval fetches (scripts/fetch_limit_small.py). Someone
# else's licence; re-fetch rather than commit.
/scripts/retrieval_eval/local/
