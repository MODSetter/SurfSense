/** Timing constants for relation scene animations. */

/** Frames each node staggers its entrance. */
export const NODE_STAGGER = 8;

/** Node fade-in duration (frames). */
export const NODE_FADE_DURATION = 20;

/** Edge draw animation duration (frames). */
export const EDGE_DRAW_DURATION = 25;

/** Camera hold per stop (frames). */
export const STOP_HOLD = 100;

/** Camera transition between stops (frames). */
export const STOP_TRANSITION = 18;
