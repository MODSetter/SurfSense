/** Timing constants for chart scene animations. */

/** Frames each bar/slice/point staggers its entrance. */
export const ITEM_STAGGER = 8;

/** Frames for each item's grow/fade-in animation. */
export const ITEM_ANIM_DURATION = 20;

/** Frames to hold the fully revealed chart before scene ends. */
export const HOLD_AFTER = 90;
