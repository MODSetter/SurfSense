/** How long the camera stays focused on each card (frames). */
export const STEP_DURATION = 220;

/** Camera transition duration between cards (frames). */
export const TRANSITION_DURATION = 40;

/** SVG stroke reveal animation duration (frames). */
export const DRAW_DURATION = 60;
