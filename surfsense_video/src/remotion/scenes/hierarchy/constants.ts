/** Hold duration on each camera stop (frames). */
export const STOP_HOLD = 90;

/** Fast snap transition between stops within a level (frames). */
export const STOP_TRANSITION = 15;

/** Transition between depth levels (frames). */
export const LEVEL_TRANSITION = 40;

/** Stagger delay per node within a level (frames). */
export const NODE_STAGGER = 8;

/** Edge draw animation duration (frames). */
export const EDGE_DRAW_DURATION = 30;

/** Node fade-in duration (frames). */
export const NODE_FADE_DURATION = 20;
