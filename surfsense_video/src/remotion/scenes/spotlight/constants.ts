/** Timing constants for spotlight scene animations. */

/** Camera hold per card (frames). */
export const STOP_HOLD = 180;

/** Camera transition between cards (frames). */
export const STOP_TRANSITION = 30;

/** SVG stroke reveal animation duration (frames). */
export const DRAW_DURATION = 50;
