/** Hold duration on each camera stop (frames @30fps ≈ 5s). */
export const STOP_HOLD = 150;

/** Fast snap transition between stops (frames). */
export const STOP_TRANSITION = 20;

/** Stagger delay per item within a visible group (frames). */
export const ITEM_STAGGER = 12;

/** Item fade-in duration (frames). */
export const ITEM_FADE_DURATION = 20;
