/** Timing constants for intro scene. */

/** Total intro duration (frames). */
export const INTRO_DURATION = 90;

/** Title reveal start (frames). */
export const TITLE_DELAY = 10;

/** Subtitle reveal start after title (frames). */
export const SUBTITLE_DELAY = 30;

/** Decorative element animation start (frames). */
export const DECOR_DELAY = 5;
