/** Timing constants for outro scene. */

/** Total outro duration (frames). */
export const OUTRO_DURATION = 75;

/** Text reveal start (frames). */
export const TEXT_DELAY = 8;

/** Fade-out start before end (frames from the end). */
export const FADEOUT_START = 20;
