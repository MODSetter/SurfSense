/** Timing constants for comparison scene animations. */

/** Frames each item staggers its entrance. */
export const ITEM_STAGGER = 6;

/** Item fade-in duration (frames). */
export const ITEM_FADE_DURATION = 18;

/** Group header stagger (frames). */
export const GROUP_STAGGER = 10;

/** Divider reveal delay (frames). */
export const DIVIDER_DELAY = 15;

/** Camera hold per stop (frames). */
export const STOP_HOLD = 110;

/** Camera transition between stops (frames). */
export const STOP_TRANSITION = 18;
